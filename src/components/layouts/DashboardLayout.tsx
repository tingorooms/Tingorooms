import { useState } from 'react';
import { Outlet, Link, useLocation, useNavigate } from 'react-router-dom';
import { useAuth } from '@/context/AuthContext';
import { Button } from '@/components/ui/button';
import { DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuSeparator, DropdownMenuTrigger,
} from '@/components/ui/dropdown-menu';
import { Sheet, SheetContent, SheetTrigger } from '@/components/ui/sheet';
import { LayoutDashboard, Building2, PlusCircle, Wallet, Users, MessageSquare, CreditCard, User, Settings, LogOut, Menu, ChevronRight
} from 'lucide-react';
import { cn, getMediaAssetUrl } from '@/lib/utils';
import NotificationBell from '@/components/layouts/NotificationBell';
import { useSiteSettings } from '@/context/SiteSettingsContext'; const DashboardLayout: React.FC = () => { const { user, logout } = useAuth(); const { settings } = useSiteSettings(); const navigate = useNavigate(); const location = useLocation(); const [isMobileMenuOpen, setIsMobileMenuOpen] = useState(false); const businessName = settings.businessName || 'RoomRental'; const logoUrl = getMediaAssetUrl(settings.logoUrl); const handleLogout = () => { logout(); navigate('/'); }; const sidebarLinks = [ { to: '/dashboard', label: 'Dashboard', icon: LayoutDashboard }, { to: '/dashboard/rooms', label: 'My Rooms', icon: Building2 }, { to: '/dashboard/rooms/post', label: 'Post Room', icon: PlusCircle }, { to: '/dashboard/expenses', label: 'Expenses', icon: Wallet }, ...(user?.role !== 'Broker' ? [{ to: '/dashboard/roommates', label: 'Roommate Group', icon: Users }] : []), { to: '/dashboard/chat', label: 'Messages', icon: MessageSquare }, ...(user?.role === 'Broker' ? [{ to: '/dashboard/plans', label: 'Choose Your Plan', icon: CreditCard }] : []), ]; const bottomLinks = [ { to: '/dashboard/profile', label: 'Profile', icon: User }, { to: '/dashboard/settings', label: 'Settings', icon: Settings }, ]; const isActive = (path: string) => { if (path === '/dashboard') { return location.pathname === '/dashboard'; } return location.pathname.startsWith(path); }; return ( <div className="min-h-screen flex bg-green-bg/30"> {/* Desktop Sidebar */} <aside className="hidden lg:flex w-72 flex-col border-r bg-white shadow-lg"> {/* Logo */} <div className="h-20 flex items-center px-6 border-b bg-gradient-to-r from-green-primary to-green-secondary"> <Link to="/" className="flex items-center gap-3 group"> {logoUrl ? ( <img src={logoUrl} alt={businessName} className="w-12 h-12 object-cover rounded-2xl group-hover:scale-110 transition-transform" /> ) : ( <div className="w-12 h-12 bg-white rounded-2xl flex items-center justify-center group-hover:scale-110 transition-transform"> <Building2 className="w-6 h-6 text-white" /> </div> )} <div className="flex flex-col"> <span className="text-xl font-extrabold text-white">{businessName}</span> <span className="text-xs text-white/80">Dashboard</span> </div> </Link> </div> {/* Navigation */} <nav className="flex-1 px-4 py-6 space-y-2 overflow-auto"> {sidebarLinks.map((link) => ( <Link key={link.to} to={link.to} className={cn( "flex items-center gap-3 px-4 py-3.5 rounded-xl text-sm font-semibold transition-all duration-300 relative group", isActive(link.to) ? "bg-gradient-to-r from-green-primary to-green-secondary text-white shadow-lg shadow-green-primary/30" : "text-gray-700 hover:bg-green-50 hover:text-green-primary" )} > <link.icon className="w-5 h-5" /> {link.label} {isActive(link.to) && ( <div className="absolute right-4 w-2 h-2 bg-white rounded-full"></div> )} </Link> ))} </nav> {/* Bottom Links */} <div className="px-4 py-4 border-t bg-gray-50 space-y-2"> {bottomLinks.map((link) => ( <Link key={link.to} to={link.to} className={cn( "flex items-center gap-3 px-4 py-3 rounded-xl text-sm font-semibold transition-all duration-300", isActive(link.to) ? "bg-gradient-to-r from-green-primary to-green-secondary text-white shadow-lg" : "text-gray-700 hover:bg-white hover:text-green-primary" )} > <link.icon className="w-5 h-5" /> {link.label} </Link> ))} <button onClick={handleLogout} className="w-full flex items-center gap-3 px-4 py-3 rounded-xl text-sm font-semibold text-red-600 hover:bg-red-50 transition-all duration-300" > <LogOut className="w-5 h-5" /> Logout </button> </div> </aside> {/* Main Content */} <div className="flex-1 flex flex-col min-w-0"> {/* Header */} <header className="h-[68px] sm:h-20 border-b bg-white flex items-center justify-between px-4 lg:px-8 shadow-sm"> <div className="flex items-center gap-4"> {/* Mobile Menu Button */} <Sheet open={isMobileMenuOpen} onOpenChange={setIsMobileMenuOpen}> <SheetTrigger className="lg:hidden"> <Menu className="h-5 w-5" /> </SheetTrigger> <SheetContent side="left" className="w-72 p-0 flex flex-col"> {/* Header */} <div className="h-16 flex items-center px-6 border-b bg-gradient-to-r from-green-primary to-green-secondary shadow-lg"> <Link to="/" className="flex items-center gap-3" onClick={() => setIsMobileMenuOpen(false)}> {logoUrl ? ( <img src={logoUrl} alt={businessName} className="w-11 h-11 object-cover rounded-2xl ring-2 ring-white/30 transition-transform hover:scale-110" /> ) : ( <div className="w-11 h-11 bg-white rounded-2xl flex items-center justify-center ring-2 ring-white/30 transition-transform hover:scale-110"> <Building2 className="w-6 h-6 text-white" /> </div> )} <div className="flex flex-col"> <span className="text-xl font-extrabold text-white">{businessName}</span> <span className="text-xs text-white/80">Dashboard</span> </div> </Link> </div> {/* Navigation */} <nav className="flex-1 overflow-y-auto px-4 py-6"> <div className="space-y-2"> <p className="text-xs font-semibold text-gray-500 uppercase tracking-wider px-3 mb-3">Main Menu</p> {sidebarLinks.map((link) => ( <Link key={link.to} to={link.to} onClick={() => setIsMobileMenuOpen(false)} className={cn( "flex items-center gap-3 px-4 py-3.5 rounded-xl text-sm font-semibold transition-all duration-300 relative group", isActive(link.to) ? "bg-gradient-to-r from-green-primary to-green-secondary text-white shadow-lg shadow-green-primary/30" : "text-gray-700 hover:bg-green-50 hover:text-green-primary hover:translate-x-1" )} > <link.icon className="w-5 h-5" /> {link.label} {isActive(link.to) && ( <span className="ml-auto w-2 h-2 bg-white rounded-full animate-pulse" /> )} </Link> ))} </div> {/* Bottom Links */} <div className="mt-8 space-y-2"> <div className="h-px bg-gray-200 my-6" /> <p className="text-xs font-semibold text-gray-500 uppercase tracking-wider px-3 mb-3">Settings</p> {bottomLinks.map((link) => ( <Link key={link.to} to={link.to} onClick={() => setIsMobileMenuOpen(false)} className={cn( "flex items-center gap-3 px-4 py-3.5 rounded-xl text-sm font-semibold transition-all duration-300 hover:translate-x-1", isActive(link.to) ? "bg-gradient-to-r from-green-primary to-green-secondary text-white shadow-lg" : "text-gray-700 hover:bg-gray-100 hover:text-gray-900" )} > <link.icon className="w-5 h-5" /> {link.label} </Link> ))} <button onClick={() => { handleLogout(); setIsMobileMenuOpen(false); }} className="w-full flex items-center gap-3 px-4 py-3.5 rounded-xl text-sm font-semibold text-red-600 hover:bg-red-50 transition-all duration-300 hover:translate-x-1" > <LogOut className="w-5 h-5" /> Logout </button> </div> </nav> {/* User Info Footer */} <div className="border-t px-4 py-4 bg-gradient-to-r from-green-50 to-green-100/50"> <div className="flex items-center gap-3"> <div className="w-10 h-10 bg-gradient-to-br from-green-primary to-green-secondary rounded-full flex items-center justify-center shadow-md"> <User className="w-5 h-5 text-white" /> </div> <div className="flex-1 min-w-0"> <p className="text-sm font-semibold text-gray-900 truncate">{user?.name}</p> <p className="text-xs text-green-600">{user?.role}</p> </div> </div> </div> </SheetContent> </Sheet> {/* Breadcrumb */} <nav className="hidden md:flex items-center gap-2 text-sm"> <Link to="/dashboard" className="text-gray-600 hover:text-green-primary font-medium transition-colors">Dashboard</Link> {location.pathname !== '/dashboard' && ( <> <ChevronRight className="w-4 h-4 text-gray-400" /> <span className="text-green-primary font-semibold capitalize"> {location.pathname.split('/').pop()?.replace('-', ' ')} </span> </> )} </nav> </div> {/* Right Side */} <div className="flex items-center gap-3"> <NotificationBell className="hover:bg-green-50" iconClassName="text-gray-700" defaultNavigatePath="/dashboard" /> <DropdownMenu> <DropdownMenuTrigger asChild><Button variant="ghost" className="hidden sm:inline-flex gap-3 hover:bg-green-50 rounded-xl px-3 h-11 border border-gray-200"> <div className="w-9 h-9 bg-gradient-to-br from-green-primary to-green-secondary rounded-xl flex items-center justify-center shadow-md"> <User className="w-5 h-5 text-white" /> </div> <div className="hidden sm:flex flex-col items-start"> <span className="text-sm font-semibold text-gray-900 leading-tight">{user?.name}</span> <span className="text-xs text-gray-500 leading-tight">{user?.role}</span> </div> </Button></DropdownMenuTrigger> <DropdownMenuContent align="end"> <DropdownMenuItem onClick={() => navigate('/dashboard/profile')}> <User className="mr-2 h-4 w-4" /> Profile </DropdownMenuItem> <DropdownMenuItem onClick={() => navigate('/dashboard/settings')}> <Settings className="mr-2 h-4 w-4" /> Settings </DropdownMenuItem> <DropdownMenuSeparator /> <DropdownMenuItem onClick={handleLogout}> <LogOut className="mr-2 h-4 w-4" /> Logout </DropdownMenuItem> </DropdownMenuContent> </DropdownMenu> </div> </header> {/* Page Content */} <main className="flex-1 p-4 lg:p-8 overflow-auto bg-gradient-to-br from-green-bg/20 via-white to-green-bg/10"> <Outlet /> </main> </div> </div> );
}; export default DashboardLayout;

